<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Login zum Rising BSM Dashboard">
  
  <title>Login - Rising BSM</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <!-- Custom Styles -->
  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      align-items: center;
      min-height: 100vh;
    }
    
    .login-wrapper {
      width: 100%;
      max-width: 400px;
      margin: 0 auto;
      padding: 15px;
    }
    
    .form-login {
      padding: 15px;
      border-radius: 10px;
      background-color: #fff;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }
    
    .form-login .form-floating:focus-within {
      z-index: 2;
    }
    
    .form-login input[type="email"] {
      margin-bottom: -1px;
      border-bottom-right-radius: 0;
      border-bottom-left-radius: 0;
    }
    
    .form-login input[type="password"] {
      margin-bottom: 10px;
      border-top-left-radius: 0;
      border-top-right-radius: 0;
    }
    
    .btn-success {
      background-color: #198754;
      border-color: #198754;
    }
    
    .btn-success:hover {
      background-color: #157347;
      border-color: #146c43;
    }
  </style>
</head>

<body>
  <main class="login-wrapper">
    <div class="text-center mb-4">
      <img src="/images/Logo.png" alt="Rising BSM Logo" width="200" class="mb-3">
      <h1 class="h3 mb-3 fw-normal">Dashboard Login</h1>
    </div>
    
    <% if (error) { %>
      <div class="alert alert-danger" role="alert">
        <i class="fas fa-exclamation-circle me-2"></i><%= error %>
      </div>
    <% } %>
    
    <% if (success) { %>
      <div class="alert alert-success" role="alert">
        <i class="fas fa-check-circle me-2"></i><%= success %>
      </div>
    <% } %>
    
    <div class="form-login">
      <form action="/login" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        
        <div class="form-floating">
          <input type="email" class="form-control" id="floatingEmail" placeholder="name@beispiel.at" name="email" required>
          <label for="floatingEmail">E-Mail Adresse</label>
        </div>
        
        <div class="form-floating">
          <input type="password" class="form-control" id="floatingPassword" placeholder="Passwort" name="password" required>
          <label for="floatingPassword">Passwort</label>
        </div>
        
        <div class="form-check text-start my-3">
          <input class="form-check-input" type="checkbox" value="remember-me" id="flexCheckDefault" name="remember">
          <label class="form-check-label" for="flexCheckDefault">
            Angemeldet bleiben
          </label>
        </div>
        
        <button class="w-100 btn btn-lg btn-success" type="submit">
          <i class="fas fa-sign-in-alt me-2"></i>Anmelden
        </button>
        
        <div class="mt-3 text-center">
          <a href="/forgot-password" class="text-decoration-none">Passwort vergessen?</a>
        </div>
      </form>
    </div>
    
    <div class="mt-4 text-center">
      <p class="text-muted">&copy; <%= new Date().getFullYear() %> Rising BSM</p>
      <p>
        <a href="/" class="text-decoration-none text-muted">
          <i class="fas fa-home me-1"></i>Zurück zur Startseite
        </a>
      </p>
    </div>
  </main>

  <!-- Bootstrap Bundle JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
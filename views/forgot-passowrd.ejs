<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Passwort vergessen - Rising BSM Dashboard">
  
  <title>Passwort vergessen - Rising BSM</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <!-- Custom Styles -->
  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      align-items: center;
      min-height: 100vh;
    }
    
    .login-wrapper {
      width: 100%;
      max-width: 400px;
      margin: 0 auto;
      padding: 15px;
    }
    
    .form-login {
      padding: 15px;
      border-radius: 10px;
      background-color: #fff;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }
    
    .btn-success {
      background-color: #198754;
      border-color: #198754;
    }
    
    .btn-success:hover {
      background-color: #157347;
      border-color: #146c43;
    }
  </style>
</head>

<body>
  <main class="login-wrapper">
    <div class="text-center mb-4">
      <img src="/images/Logo.png" alt="Rising BSM Logo" width="200" class="mb-3">
      <h1 class="h3 mb-3 fw-normal">Passwort vergessen</h1>
      <p class="text-muted">Geben Sie Ihre E-Mail-Adresse ein, um einen Link zum Zurücksetzen Ihres Passworts zu erhalten.</p>
    </div>
    
    <% if (error) { %>
      <div class="alert alert-danger" role="alert">
        <i class="fas fa-exclamation-circle me-2"></i><%= error %>
      </div>
    <% } %>
    
    <% if (success) { %>
      <div class="alert alert-success" role="alert">
        <i class="fas fa-check-circle me-2"></i><%= success %>
      </div>
    <% } %>
    
    <div class="form-login">
      <form action="/forgot-password" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        
        <div class="form-floating mb-3">
          <input type="email" class="form-control" id="floatingEmail" placeholder="name@beispiel.at" name="email" required>
          <label for="floatingEmail">E-Mail Adresse</label>
        </div>
        
        <button class="w-100 btn btn-lg btn-success" type="submit">
          <i class="fas fa-paper-plane me-2"></i>Link senden
        </button>
        
        <div class="mt-3 text-center">
          <a href="/login" class="text-decoration-none">Zurück zum Login</a>
        </div>
      </form>
    </div>
    
    <div class="mt-4 text-center">
      <p class="text-muted">&copy; <%= new Date().getFullYear() %> Rising BSM</p>
      <p>
        <a href="/" class="text-decoration-none text-muted">
          <i class="fas fa-home me-1"></i>Zurück zur Startseite
        </a>
      </p>
    </div>
  </main>

  <!-- Bootstrap Bundle JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
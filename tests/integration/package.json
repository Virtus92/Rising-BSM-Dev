{
  "name": "rising-bsm-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for Rising-BSM API",
  "main": "api.test.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "verbose": true,
    "testTimeout": 30000,
    "setupFiles": [
      "dotenv/config"
    ]
  }
}
